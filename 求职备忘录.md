1、自我介绍：个人信息部分省略，着重介绍一下重点项目和熟悉的技术方向，加上项目介绍和技术栈内容，体现自己信息和优势点
2、注重一下Java基础知识的问题，归纳总结出技术要点，能举具体案例，加强一下基础知识和归纳能力。
3、优化一下简历，技术写的再具体点，同样可以针对某一个自己擅长的领域重点描述下，一定要熟悉自己简历上的项目和技术要点

Redis场景：
临时数据存储：滑块经验参数临时存储
数据库减负：存用户信息  zsort热门数据top5  点赞量
分布式锁：自动任务AOP中，禁止相同任务同时执行；注册时禁止相同用户同时注册；Redisson框架实现(多数生产环境)/RedLock算法(多redis节点容错)/redis+Lua保证原子性(简单生产环境)

困难：
广汉项目： 是一个提前进场的项目； 甲方 需求频繁变动、对功能的布局 美观度 系统不满意
要求对多个系统进行融合到一个系统，系统整合过程中遇到了很多不可控的因素（比如：两个系统间 版本不兼容 ）

@Transactional失效情况：
注解未使用public修饰方法(AOP默认代理public修饰的方法)
同类内部调用会绕过代理（同一个类中，非注解方法调用注解方法）
@Transactional方法中使用try-catch捕获异常，且不在catch中抛出异常（要么不使用try-catch,要么在catch中抛出异常）

 
自定义Zuul拦截：重写ZuulFilter的run方法；获取当前请求内容，根据请求的URI判断是否属于白名单，是放行；
                否则对token进行有效性校验并解析，获取用户信息，检査token是否需要续期；重新封装请求头信息调用真实接口


使用事物导致锁表：
        sqlserver数据库嵌套事物，导致锁表（sqlserver默认事物隔离级别为 已提交读；在嵌套事物中 不可重复读）   调整嵌套事物的隔离级别避免锁表、避免使用嵌套事物
        mysql默认隔离级别 可重复读，可以使用嵌套事物
        业务场景：对账-发票确认时 需要推送SAP 文档 金税 多方系统，并回写数据到SRM，并且是穿行的前一个接口返回值 作为下一个接口的入参，就使用了嵌套事物

第三家公司
公司业务：保险业务
参与项目：国家知识产权保险综合服务项目
主要功能：
        注册登录：注册对敏感信息双层加密（RSA整体加密 AES字段加密）,接入统一用户中心; 登录滑块安全经验，Zuul拦截和鉴权，生成jwttoken；
        政策发布/案例展示/产品信息：后台可以对知识产权相关政策、案例、产品信息进行维护并发布；发布成功后，前台页面可以查看最新的政策信息
        需求匹配：根据用户发布的知识产权保险需求 保险机构及时匹配需求并响应
        基础服务：法律服务 咨询服务 培训服务 大屏统计 
        自定义审批流：流程配置(模版基本信息表 节点表  岗位表 前后置方法表) 流程审批(对增删改所有方法，加流程启动自定义注解，AOP对使用了自定义注解的方法进行流程启动，根据不同的操作类型进行节点流转，并生产流程轨迹)
        字段脱敏：使用AOP对使用了自定义注解的方法，进行后置增强；对目标方法的返回结果中加了自定义注解的字段就行脱敏；双击解脱敏，通过调用接口传入字段名称、主键、表名查对应业务表的字段
        系统权限管控：功能模块与角色关联，当前模块只对与关联模块匹配的用户开放
        系统操作轨迹：将当前用户信息及操作前后数据保存到轨迹表
        数据库字段加密：继承BaseTypeHandler抽象类，重写setNonNullParameter方法，将字段进行AES加密保存到数据库；重写getNullableResult方法，将字段进行解密返回；
主要工作：设计 编码 自测 
使用技术：spring、springboot、mybatis、springcloud相关组件（consul openfeign zull apollo） XXLJob(分布式定时任务) redis 高斯数据库



第二家公司 
公司业务：政务服务
参与项目: 大大小小参与了十多个项目的建设；主要负责政务服务相关项目，其中本人负责的项目有 重庆荣昌政务服务项目、广汉政务服务项目、新都政务服务项目
主要功能：根据维护的事项进行办件的申报（如企业登记、变更、注销；个人：身份证、结婚证、出生证明、入学），包含线上线下渠道。办件系统还衍生出了数据大屏、 智能客服 、 排队叫号、 一件事 等相关子系统。
主要工作：设计 编码 自测；把握项目整体开发进度
使用技术: 后端基于spring框架，前端基于miniUI； 
        其中数据库使用了mysql、redis、mongoDB; 
        由于办件量比较大还对办件数据采用了分表；
        redis存用户信息，因为用户信息变更少而且使用频率高；还用于存储一些热点数据。
        mongodb用于存储办件后生成的电子证照数据。
        rabbitmq：异步处理一些非主线的逻辑；比如办件流程中的短信，某些数据的推送。
        job：主要用于处理些定时任务，比如已完成的办件定时迁移到历史表；定时发送短信；在晚上做一下数据的推送或者同步；
        多线程：主要用于处理大批量数据；比如导出、事项数据的质检


第一家公司
公司业务：住房公积金业务，其中包含缴存、提取、贷款相关业务
参与项目：只参与了成都住房公积金改造升级项目；主要负责缴存相关功能，由于那会刚毕业 参与的都是一些非核心的业务
使用技术：后端基于spring框架搭建、前段使用阿里的react框架
主要功能：汇缴、补缴、个人账号设立、个人缴存状态变更、单位业务流水查询、打印相关凭证

