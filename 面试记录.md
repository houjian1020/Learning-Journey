总结：
    增强jvm排查实战能力
    增强springcloud组件的应用能力
    


上海菱众面试题：
com.springboot.demo.interview.questions.lingzong.Test1
com.springboot.demo.interview.questions.lingzong.Test2
com.springboot.demo.interview.questions.lingzong.Test3


成都汇众星科技：
SpringMVC工作原理
IOC原理
AOP原理
Mybatis一二及缓存
OpenFeign
Gateway
Nacos
Mysql数据库引擎、索引优化
Redis五大基本数据类型
分布式锁：Redisson框架实现getLock()  lock(支持超时和自动续期)   unlock()
了解布隆过滤器
redis持久化、集群
Linux常用命令
Nginx负载均衡


亚信科技：通信
1、工作流框架
2、分布式锁
3、Redis分片 哨兵  集群
4、如何提高并发量：缓存、分库分表、集群、读写分离


成都杰瑞达科技有限公司：医疗
mybatis aes加密身份证，如何模糊查询 ： 分词加密，如果按4位分词
mybatis 的自带<update  id=''> 怎么用 批量更新 乐观锁
两个表字段相同，一个加联合索引  一个加独立索引 联合查询时，都查首字段 哪个更快 ？   联合索引表更快，原因是查询字段包含在索引中，减少了回本次数
nacos如何实时同步 ： 客户端向服务端发送 http 长轮询 请求； 实现方式：在需要动态刷新的Bean中添加@RefreshScope注解
mybatis和mybatisPlus区别：手动SQL—内置基础SQL    XML中配置SQL——通过对象链式拼接 
#{} 和 ${} 区别： 预编译（占位符）—字符串拼接     防止SQL注入—存在SQL入风险
@RestController 与 @Controller 有哪些区别 ：@Controller 返回的是视图   @RestController 返回的是json ， 是@Controller + @ResponseBody
Redis 分布式锁  类型   使用场景
数据库：索引  回表
线程安全的集合 、常用集合
@Autowired 默认按类型匹配  @Resource 按名称匹配




工行外包：
一面：
    sleep与wait区别：释放锁
    讲讲ConcurrentHashMap： 锁机制CAS+synchronized
    JVM中的垃圾回收机制: 对象与引用断开、 回收条件：老年代 元空间满 system.gc()
    `项目中是否遇到OOM，如何跟踪排查`：日志定位现象 → Arthas(阿尔萨斯-阿里的) 实时诊断 → MAT 深度分析 → 监控长期预警 → 代码/架构优化根因（异步-分批处理 导入）
    索引失效场景: 最左前缀、字段使用函数/运算、like、范围查询破坏后续字段、隐式类型转换、or、结果比例大
    SQL优化：建索引+explain执行计划
    线程池7个参数
   ` Redission实现方式、如何保证原子性`：； Lua 脚本 + Redis 单线程 + Hash（key锁名 Field持锁线程 value重入次数） 结构保证了原子性； 还支持：可重入锁、公平锁、读写锁；内置了看门狗（Watchdog）机制
    事物传播行为有哪几种：7种

二面：
    加强微服务中间件使用的学习：
    分库分表、读写分离策略熟悉： ShardingSphere-JDBC 
    简历中的知识产权项目经历
    如何保证多数据库的一个一致性： `分布式事物（强一致性）、消息队列（最终一致）、分布式锁（业务层）、 Binlog 实时同步`
    spring中如何解决循环依赖：`三级缓存机制‌ 解决 ‌单例 Bean 的属性注入（无法解决构造器注入）‌ 循环依赖`
    项目中最大的挑战：



华晟国康：
    sentinel的限流、降级配置，注解包含哪些参数，注解是加在controller还是service： value、blockHandler、fallback、blockHandlerClass / fallbackClass、defaultFallback、exceptionsToIgnore；  推荐Service 层，controller也可以
    如何自定义starter： 核心是编写自动配置类：@Configuration   @EnableConfigurationProperties    @ConditionalOnClass    @ConditionalOnProperty
    springcloud常用注解有哪些：
        服务注册与发现：@EnableDiscoveryClient  启动类上使用
        配置中心： @RefreshScope    @Value   @ConfigurationProperties
        负载均衡：@LoadBalanced   @LoadBalancerClient   @LoadBalancerClients
        服务调用：@EnableFeignClients   @FeignClient     @GetMapping / @PostMapping
        限流与熔断降级：@SentinelResource
        全局分布式事务： @GlobalTransactional
    消息队列如何保证操作的最终一致性：方案一（本地消息表 + 定时任务（生产者）未处理的数据记录 + 消费者处理执行业务 + 消费失败的重试/人工介入）   本质：生产者不丢失消息  消费者不重复消费  异常的补偿处理 就可以保证最终一致性
    索引a b 查询条件：a>xxx and b=yyy  索引是否失效： 部分失效，范围查找 会导致后续索引失效
    幂等：无论操作多少次，结果都不发生变化（前端的防止连击、后端唯一主键防止重复提交、MQ唯一标识防止重复消费）
    幂等：
        概念：无论你执行多少次，最终结果都和执行一次完全一样； 查询、删除天然幂等，新增、更新是问题重灾区
        新增：唯一约束 禁止重复插入
        修改：版本控制
        Redis令牌： 变更时，判断Token令牌是否存在Redis中，不存在说明是重复消费



聚创医疗：
    一面：MySQL（主从 读写分离  分库分表  分区  索引  优化策略）   Redis（类型及适应场景  分布式锁及底层实现   淘汰策略  持久化策略   ）   高并发解决方案    分布式事物   springboot（AOP IOC  启动  注解）
        主从同步如何实现: 
            原理：主库 收到数据变更后，写入binlog； 从库 连接主库，请求 Binlog 内容，收到后将它们写入从库本地的 Relay Log
            数据库配置：修改my.cnf文件-----主库创建复制账号------从库设置主库信息------从库启动同步
            代码实现：引依赖：连接池----改yml：master/slave-----AOP判断走主从------ 一致性：写后读强一致性场景，读写都走主库(自定义注解@Master)
            同步模式： 异步复制（默认 一致性差  性能高）   半同步复制（至少一个从库一致   一致性较高  性能中等）   强同步（所有从库一致   一致性极高   性能差）
        分区与分库分表：
            区别：分区是“单机内”的物理拆分，分库分表是“多机间”的分布式拆分
            实现方式、拆分方式
        高并发-秒杀功能 如何提`高并发`量： Sentinel 限流、 缓存、 MQ（削峰填谷，异步下单）、 数据库（读写分离 分库分表 缓存）
        如何保证`主从同步一致性`： 数据库层（异步复制  半同步复制  强同步）   应用层（特定场景：修改后读、事物等 强制读主库）
        如何设计一个`工厂`模式： 将对象的 创建逻辑 与 业务逻辑 解耦，通过一个“工厂”来统一管理和创建对象，从而遵循开闭原则（对扩展开放，对修改关闭）
        如何保证单例模式


迈格森教育：
    一面：集合底层 锁底层
        单例模式的代码，及其他设计模式的设计原理及应用场景（工厂 代理）
            单例：私有+静态+volatile修饰对象   私有构造方法    私有方法创建对象：双重校验+锁
        注重锁相关底层原理（ volatile-可见性 禁止指令重排序原理  sycn  lock  分布式锁：锁超时怎么设置合理，看门狗如何实现 ）
        微服务架构作用：解耦 隔离容错  弹性扩容
        系统有哪些拆分方式：单一职责（纵向拆分：用户 订单 商品  支付）  公共服务（横向拆分：短信 文件上传 权限等公共功能 设计到中台）  数据量（读写分离  分库分表）   高并发
        MQ消息积压如何解决，什么时候用pull push？
            原因：消费者消费能力不足  消费者异常  突然并发
            方案：Push模式下的 多消费者消费  且 调整Prefetch-预取值 （推荐方式）、   如果消费业务逻辑太重，消费者先落地，业务处理异步或者定时任务处理  、  突发高并发:限流
            推荐：push（99%场景）  pull网络开销大（定时、批量、离线）
        arrayList与LinkedList区别，如何把arrayList与LinkedList性能结合
            非线程安全：HashMap  LinkedHashMap  TreeMap
            线程安全：ConcurrentHashMap
        CopyOnWriteArrayList原理：在多读少写的场景，可以保证线程安全 和 高性能
            原理：volatile修饰数组 保证修改的可见性；读无锁，可能读到的不是最新修改； 写 上锁（防止多线程同时复制）+复制 ； 保证的是最终一致性
        ConcurrentHashMap底层原理如何保证线程安全    
        红黑树特点，为什么链表超过8要转红黑树 ，为什么不用平衡二叉树
        ----AI相关---
        归一化作用是什么
        温度参数作用是什么
        提示词如何优化
        如何避免幻觉
        MCP Skills  tools区别





